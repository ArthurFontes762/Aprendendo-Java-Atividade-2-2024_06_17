// Essa é a classe para uma funcionalidade a minha escolha utilizando Math.max ou Math.min
class OutraFuncionalidade {
    public static double calcularMaximo(double a, double b) {
        return Math.max(a, b);
    }
}